# Runtime dependencies of backend/server.py (direct imports plus what FastAPI needs at runtime).
# Versions are the ones the test suite was run against (Python 3.13).
fastapi==0.110.1
starlette==0.37.2
uvicorn==0.25.0
pydantic==2.13.5
email-validator==2.3.0        # required by pydantic's EmailStr
python-multipart==0.0.32      # required by FastAPI for file uploads (/api/upload)
motor==3.3.1
pymongo==4.6.3
bcrypt==5.0.0                 # 5.x rejects passwords over 72 bytes; server.py validates this
python-dotenv==1.2.3
httpx==0.28.1                 # Google token exchange + Open Library search
